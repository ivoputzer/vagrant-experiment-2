Vagrant Experiment
---


```sh
vagrant init
# changed the box to "ubuntu/focal64"
vagrant up
vagrant destroy

vagrant up
vagrant halt

vagrant up
# changed the configuration file
vagrant reload

vagrant suspend
vagrant resume
```
